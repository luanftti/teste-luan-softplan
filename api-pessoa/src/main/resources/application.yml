spring:
    application:
        name: api-pessoa
    profiles:
        active: dev
    security:
        user:
            name: api_user
            password: api@Tester2025
    datasource:
        driver-class-name: org.postgresql.Driver
        password: api@2025$UU
        username: api_user
        url: jdbc:postgresql://localhost:5432/postgres
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect